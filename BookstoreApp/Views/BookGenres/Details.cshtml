@model BookstoreApp.Models.BookGenre

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>BookGenre</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Book)
        </dt>
        <dd class = "col-sm-10">
            <a asp-controller="Books" asp-action="Details" asp-route-id="@Model.BookId"> @Html.DisplayFor(model => model.Book.Title) </a>
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Genre)
        </dt>
        <dd class = "col-sm-10">
            <a asp-controller="Genres" asp-action="Details" asp-route-id="@Model.GenreId"> @Html.DisplayFor(model => model.Genre.GenreName) </a>
        </dd>
    </dl>
</div>
<div>
    @if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
    {
        <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a>
    }
    <a asp-action="Index">Back to List</a>
</div>
